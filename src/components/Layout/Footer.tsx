import styles from "../../styles/Layout.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p>GIT</p>
      <p>Sebastian</p>
    </footer>
  );
};

export default Footer;
